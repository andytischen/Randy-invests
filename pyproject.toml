[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "randy-invests"
version = "0.3.0"
description = "ML-powered stock market predictor with a retail investor tiers portal"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.9"
dependencies = [
    "yfinance>=0.2.28",
    "pandas>=2.0.0",
    "numpy>=1.24.0",
    "scikit-learn>=1.3.0",
    "ta>=0.10.2",
    "requests>=2.31.0",
    "joblib>=1.3.0",
    "pyarrow>=12.0.0",
    "flask>=3.0.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
    "pytest-cov>=4.0",
    "ruff>=0.4.0",
]

[project.scripts]
randy-invests = "randy_invests.main:main"
randy-invests-portal = "randy_invests.web.app:main"

[tool.setuptools.packages.find]
where = ["."]
include = ["randy_invests*"]

[tool.setuptools.package-data]
"randy_invests.web" = ["templates/*.html", "static/css/*", "static/js/*", "static/img/*"]

[tool.ruff]
line-length = 100
target-version = "py39"

[tool.ruff.lint]
select = ["E", "F", "W", "I", "UP"]
ignore = ["E501"]

[tool.pytest.ini_options]
testpaths = ["randy_invests/tests"]
